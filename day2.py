# use cartesian coordinates

grid = [[1, 2, 3],
        [4, 5, 6],
        [7, 8, 9]]

def less_one(in_var):
        if in_var - 1 <= 0:
                return in_var
        else:
             return in_var - 1


def more_one(in_var):
        if in_var + 1 >= 2:
                return in_var
        else:
                return in_var +1

def 